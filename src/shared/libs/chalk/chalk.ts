import chalk, { ChalkInstance } from 'chalk';

export const errorColor: ChalkInstance = chalk.bold.red;
export const warningColor: ChalkInstance = chalk.hex('#FFA500');
export const successColor: ChalkInstance = chalk.green;
